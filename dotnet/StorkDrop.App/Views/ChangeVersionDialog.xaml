<Window
  x:Class="StorkDrop.App.Views.ChangeVersionDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="{DynamicResource ChangeVersion_Title}"
  Width="480"
  MaxHeight="560"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
  Background="{DynamicResource BackgroundBrush}"
>
  <StackPanel Margin="24">
    <TextBlock
      Text="{DynamicResource ChangeVersion_Title}"
      FontSize="18"
      FontWeight="SemiBold"
      Margin="0,0,0,12"
    />
    <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,16" />

    <ListBox
      x:Name="VersionList"
      MaxHeight="320"
      SelectionChanged="VersionList_SelectionChanged"
      Background="{DynamicResource SurfaceBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
    >
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Grid Margin="4,3">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
              Grid.Column="0"
              Text="{Binding Version}"
              FontSize="13"
              VerticalAlignment="Center"
            />
            <TextBlock
              Grid.Column="1"
              Text="{DynamicResource ChangeVersion_Installed}"
              FontSize="11"
              Opacity="0.6"
              VerticalAlignment="Center"
              Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVisibilityConverter}}"
            />
          </Grid>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button
        Content="{DynamicResource ChangeVersion_Cancel}"
        Style="{DynamicResource OutlinedButton}"
        Padding="16,8"
        Margin="0,0,8,0"
        Click="Cancel_Click"
      />
      <Button
        x:Name="ApplyButton"
        Content="{DynamicResource ChangeVersion_Apply}"
        Style="{DynamicResource FilledAccentButton}"
        Padding="16,8"
        IsEnabled="False"
        Click="Apply_Click"
      />
    </StackPanel>
  </StackPanel>
</Window>
