<Window
  x:Class="StorkDrop.App.Views.InstallLogWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="{Binding Title, StringFormat='Installation Log - {0}'}"
  Icon="pack://application:,,,/StorkDrop.App;component/Assets/stork.ico"
  Width="650"
  Height="450"
  WindowStartupLocation="CenterOwner"
  Background="White"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Background="#F5F5F5" Padding="16,12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
          Text="{Binding Title}"
          FontSize="16"
          FontWeight="SemiBold"
          VerticalAlignment="Center"
        />
        <TextBlock
          Grid.Column="1"
          Text="{Binding StatusMessage}"
          FontSize="12"
          Foreground="#666666"
          TextTrimming="CharacterEllipsis"
          HorizontalAlignment="Right"
          VerticalAlignment="Center"
          Margin="16,0,16,0"
        />
        <Button
          Grid.Column="2"
          x:Name="CancelButton"
          Content="{DynamicResource Cancel_Install_Button}"
          Click="CancelButton_Click"
          VerticalAlignment="Center"
        >
          <Button.Style>
            <Style TargetType="Button" BasedOn="{StaticResource FilledAccentButton}">
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                  <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Button.Style>
        </Button>
      </Grid>
    </Border>

    <!-- Progress -->
    <ProgressBar Grid.Row="1" Value="{Binding Percentage}" Maximum="100" Height="4">
      <ProgressBar.Style>
        <Style TargetType="ProgressBar" BasedOn="{StaticResource StyledProgressBar}">
          <Setter Property="IsIndeterminate" Value="False" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
              <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Percentage}" Value="0">
              <Setter Property="IsIndeterminate" Value="True" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </ProgressBar.Style>
    </ProgressBar>

    <!-- Log entries -->
    <TextBox
      Grid.Row="2"
      x:Name="LogTextBox"
      FontFamily="Consolas"
      FontSize="12"
      Background="#1E1E1E"
      Foreground="#DCDCDC"
      BorderThickness="0"
      IsReadOnly="True"
      TextWrapping="NoWrap"
      AcceptsReturn="True"
      VerticalScrollBarVisibility="Auto"
      HorizontalScrollBarVisibility="Auto"
      Padding="8,4"
    />
  </Grid>
</Window>
