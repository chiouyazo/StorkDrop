<Window
  x:Class="StorkDrop.App.Views.SelectInstanceDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="{DynamicResource SelectInstance_Title}"
  Width="520"
  Height="Auto"
  SizeToContent="Height"
  MinWidth="420"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
  Background="{DynamicResource BackgroundBrush}"
>
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock
      Grid.Row="0"
      Text="{DynamicResource SelectInstance_Title}"
      FontSize="18"
      FontWeight="SemiBold"
      Margin="0,0,0,12"
    />
    <TextBlock
      Grid.Row="1"
      x:Name="MessageText"
      TextWrapping="Wrap"
      FontSize="13"
      Margin="0,0,0,16"
    />

    <ComboBox
      Grid.Row="2"
      x:Name="InstanceBox"
      SelectionChanged="InstanceBox_SelectionChanged"
      Background="{DynamicResource SurfaceBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
      Padding="8,6"
    >
      <ComboBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Margin="2">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" />
              <TextBlock
                Text="{Binding Version, StringFormat='v{0}'}"
                FontSize="12"
                Opacity="0.7"
                Margin="8,0,0,0"
              />
            </StackPanel>
            <TextBlock
              Text="{Binding InstalledPath}"
              FontSize="11"
              Opacity="0.6"
              TextTrimming="CharacterEllipsis"
            />
          </StackPanel>
        </DataTemplate>
      </ComboBox.ItemTemplate>
    </ComboBox>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button
        Content="{DynamicResource ChangeVersion_Cancel}"
        Style="{DynamicResource OutlinedButton}"
        Padding="16,8"
        Margin="0,0,8,0"
        Click="Cancel_Click"
      />
      <Button
        x:Name="ApplyButton"
        Content="{DynamicResource SelectInstance_Apply}"
        Style="{DynamicResource FilledAccentButton}"
        Padding="16,8"
        IsEnabled="False"
        Click="Apply_Click"
      />
    </StackPanel>
  </Grid>
</Window>
