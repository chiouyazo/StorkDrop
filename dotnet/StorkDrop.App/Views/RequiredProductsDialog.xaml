<Window
  x:Class="StorkDrop.App.Views.RequiredProductsDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="{DynamicResource RequiredComponents_Title}"
  Width="500"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
  Background="{DynamicResource BackgroundBrush}"
>
  <StackPanel Margin="24">
    <TextBlock
      Text="{DynamicResource RequiredComponents_Title}"
      FontSize="18"
      FontWeight="SemiBold"
      Margin="0,0,0,12"
    />
    <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,16" />
    <ItemsControl x:Name="ProductList" Margin="0,0,0,8">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <CheckBox
            IsChecked="{Binding IsSelected}"
            IsEnabled="{Binding IsInstalled, Converter={StaticResource InverseBoolConverter}}"
            Margin="4,4"
            FontSize="13"
          >
            <StackPanel>
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                <TextBlock
                  Text="{DynamicResource OptionalProducts_AlreadyInstalled}"
                  FontSize="11"
                  Opacity="0.6"
                  Margin="8,0,0,0"
                  VerticalAlignment="Center"
                  Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibilityConverter}}"
                />
              </StackPanel>
              <TextBlock
                Text="{Binding Description}"
                FontSize="12"
                Opacity="0.7"
                TextWrapping="Wrap"
                Visibility="{Binding HasDescription, Converter={StaticResource BoolToVisibilityConverter}}"
              />
            </StackPanel>
          </CheckBox>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <ItemsControl x:Name="UnavailableList" Margin="0,0,0,16">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding}" FontSize="13" Margin="8,2" Foreground="#D32F2F" />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button
        Content="{DynamicResource RequiredProducts_SkipContinue}"
        Style="{DynamicResource OutlinedButton}"
        Padding="16,8"
        Margin="0,0,8,0"
        Click="Skip_Click"
      />
      <Button
        Content="{DynamicResource RequiredProducts_InstallContinue}"
        Style="{DynamicResource FilledAccentButton}"
        Padding="16,8"
        Click="Install_Click"
      />
    </StackPanel>
  </StackPanel>
</Window>
